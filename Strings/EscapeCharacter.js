//escape character \t \r\n
console.log("escape \n characters") //\nnext line\tnext tab
let str = "apna\ncollege"
console.log(str.length) //it will not display 13 because \n it will consider as a single character so it will display 12.
